# Quick start

1. Install yarn:

```bash
npm i -g yarn
brew install yarn
```
2. Install parcel v2:

```bash
yarn add parcel --dev
```
3. Run project:

```bash
yarn parcel src/index.html
```

4. Open in browser http://localhost:1234
